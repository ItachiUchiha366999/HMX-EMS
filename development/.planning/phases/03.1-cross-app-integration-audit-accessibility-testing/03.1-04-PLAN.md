---
phase: 03.1-cross-app-integration-audit-accessibility-testing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frappe-bench/apps/university_erp/university_erp/scripts/migrate_custom_fields.py
  - frappe-bench/apps/university_erp/university_erp/university_analytics/report/student_performance_analysis/student_performance_analysis.py
  - frappe-bench/apps/university_erp/university_erp/university_feedback/report/feedback_analysis/feedback_analysis.py
  - frappe-bench/apps/university_erp/university_erp/university_grievance/report/sla_compliance_report/sla_compliance_report.py
  - frappe-bench/apps/university_erp/university_erp/university_payments/report/refund_report/refund_report.py
  - frappe-bench/apps/university_erp/university_erp/university_placement/report/placement_trend/placement_trend.py
  - frappe-bench/apps/university_erp/university_erp/university_placement/report/company_wise_placement/company_wise_placement.py
  - frappe-bench/apps/university_erp/university_erp/university_placement/report/program_wise_placement/program_wise_placement.py
  - frappe-bench/apps/university_erp/university_erp/university_examinations/report/examination_result_analysis/examination_result_analysis.json
  - frappe-bench/apps/university_erp/university_erp/university_finance/report/program_wise_fee_report/program_wise_fee_report.json
  - frappe-bench/apps/university_erp/university_erp/university_library/report/overdue_books/overdue_books.py
  - frappe-bench/apps/university_erp/university_erp/university_erp/analytics/dashboards/executive_dashboard.py
  - frappe-bench/apps/university_erp/university_erp/university_erp/doctype/university_settings/university_settings.json
autonomous: true
gap_closure: true
requirements: [GAP-01, GAP-02, GAP-03, GAP-04, GAP-05, AUDIT-RPT, AUDIT-API]

must_haves:
  truths:
    - "All 13 Employee custom fields exist as columns in the tabEmployee database table"
    - "All 9 Leave Application custom fields exist as columns in the tabLeave Application database table"
    - "All 14 previously-failing university_erp reports execute without error via frappe.desk.query_report.run()"
    - "All 27 faculty API endpoints return valid responses (not MySQL 1054 errors)"
    - "Executive dashboard get_overview_metrics() returns metrics without SQL error"
  artifacts:
    - path: "frappe-bench/apps/university_erp/university_erp/scripts/migrate_custom_fields.py"
      provides: "Custom field migration script that loads fixture JSONs and creates Custom Field records"
      contains: "employee_custom_fields.json"
    - path: ".planning/phases/03.1-cross-app-integration-audit-accessibility-testing/REPORT_AUDIT.md"
      provides: "Updated report audit showing 14 previously-failing reports now PASS"
    - path: ".planning/phases/03.1-cross-app-integration-audit-accessibility-testing/API_AUDIT.md"
      provides: "Updated API audit showing 27/27 endpoints PASS (or with documented data-dependent failures)"
  key_links:
    - from: "migrate_custom_fields.py"
      to: "employee_custom_fields.json"
      via: "json.load of fixture file"
      pattern: "employee_custom_fields\\.json"
    - from: "migrate_custom_fields.py"
      to: "leave_application_custom_fields.json"
      via: "json.load of fixture file"
      pattern: "leave_application_custom_fields\\.json"
    - from: "faculty_api.py get_current_faculty()"
      to: "tabEmployee.custom_is_faculty"
      via: "frappe.db.get_value with custom_is_faculty=1"
      pattern: "custom_is_faculty"
---

<objective>
Migrate all custom fields to the database, fix all 14 failing reports and the executive dashboard, then re-run audit scripts to verify everything passes.

Purpose: This is the root cause fix that unblocks the faculty portal (27 API endpoints) and fixes 14 broken reports. Without custom fields in the DB, the entire faculty portal is non-functional. Without report fixes, staff cannot use 14 analytics reports.

Output: Updated REPORT_AUDIT.md and API_AUDIT.md showing green status, working faculty API, working reports.
</objective>

<execution_context>
@/workspace/development/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/development/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-cross-app-integration-audit-accessibility-testing/03.1-CONTEXT.md
@.planning/phases/03.1-cross-app-integration-audit-accessibility-testing/03.1-02-SUMMARY.md
@.planning/phases/03.1-cross-app-integration-audit-accessibility-testing/REPORT_AUDIT.md
@.planning/phases/03.1-cross-app-integration-audit-accessibility-testing/API_AUDIT.md

<interfaces>
<!-- Custom field fixture format (both files follow this structure) -->
From frappe-bench/apps/university_erp/university_erp/faculty_management/fixtures/employee_custom_fields.json:
13 Custom Field records for Employee doctype:
- custom_faculty_section (Section Break)
- custom_is_faculty (Check, default "0")
- custom_faculty_profile (Link to Faculty Profile, read_only)
- custom_employee_category (Select: Teaching/Non-Teaching/Administrative/Contract/Visiting)
- custom_column_break_faculty (Column Break)
- custom_faculty_type (Select: Permanent/Temporary/Adhoc/Guest)
- custom_current_workload (Float, precision 2, read_only)
- custom_qualification_section (Section Break)
- custom_qualifications (Table -> Employee Qualification)
- custom_highest_qualification (Data)
- custom_specialization (Data)
- custom_experience_section (Section Break)
- custom_total_experience (Float, precision 1)
- custom_teaching_experience (Float, precision 1)

From frappe-bench/apps/university_erp/university_erp/faculty_management/fixtures/leave_application_custom_fields.json:
9 Custom Field records for Leave Application doctype:
- custom_academic_impact_section (Section Break)
- custom_affects_teaching (Check, default "0")
- custom_total_classes_affected (Int, read_only)
- custom_column_break_academic (Column Break)
- custom_substitute_arranged (Check, default "0")
- custom_hod_notified (Check, read_only, default "0")
- custom_affected_courses_section (Section Break, depends_on)
- custom_affected_courses (Table -> Leave Affected Course)
- custom_temporary_assignment (Link to Temporary Teaching Assignment, read_only)

<!-- Report module path resolution -->
Frappe resolves report module paths as: {app}.{module_scrubbed}.report.{report_scrubbed}.{report_scrubbed}
- "University Examinations" -> university_examinations
- "University Finance" -> university_finance
Report JSON module field must match an entry in modules.txt exactly.

<!-- Executive dashboard status column issue -->
From executive_dashboard.py line 376:
frappe.db.count("Student Batch Name", {"status": "Active"})
The Student Batch Name doctype (Education module) does not have a "status" field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate custom fields to database and fix executive dashboard</name>
  <files>
    frappe-bench/apps/university_erp/university_erp/scripts/migrate_custom_fields.py,
    frappe-bench/apps/university_erp/university_erp/university_erp/analytics/dashboards/executive_dashboard.py,
    frappe-bench/apps/university_erp/university_erp/university_erp/doctype/university_settings/university_settings.json
  </files>
  <read_first>
    frappe-bench/apps/university_erp/university_erp/faculty_management/fixtures/employee_custom_fields.json,
    frappe-bench/apps/university_erp/university_erp/faculty_management/fixtures/leave_application_custom_fields.json,
    frappe-bench/apps/university_erp/university_erp/university_erp/analytics/dashboards/executive_dashboard.py,
    frappe-bench/apps/university_erp/university_erp/university_erp/doctype/university_settings/university_settings.json
  </read_first>
  <action>
    **1. Create migrate_custom_fields.py script** at `frappe-bench/apps/university_erp/university_erp/scripts/migrate_custom_fields.py`:

    The script must:
    a. Load `employee_custom_fields.json` (13 fields for Employee doctype) and `leave_application_custom_fields.json` (9 fields for Leave Application doctype) using `json.load()` from the fixtures directory at `../faculty_management/fixtures/` relative to the scripts directory.
    b. For each field definition in both JSON arrays, check if a Custom Field record already exists via `frappe.db.exists("Custom Field", field["name"])`.
    c. If not exists, create it via `cf = frappe.new_doc("Custom Field")`, set all properties from the JSON (dt, fieldname, fieldtype, label, options, insert_after, default, read_only, precision, depends_on), then `cf.flags.ignore_permissions = True` and `cf.insert()`.
    d. If exists, update it via `frappe.get_doc("Custom Field", field["name"])` and set all properties, then `.save()`.
    e. After processing all fields, call `frappe.db.commit()`.
    f. Print summary: "Migrated X Employee custom fields, Y Leave Application custom fields".
    g. Include a `def run():` entry point (for `bench --site university.local execute university_erp.scripts.migrate_custom_fields.run`).

    After creating the script, execute it:
    ```bash
    cd /workspace/development/frappe-bench && bench --site university.local execute university_erp.scripts.migrate_custom_fields.run
    ```
    Then run bench migrate to sync columns:
    ```bash
    cd /workspace/development/frappe-bench && bench --site university.local migrate
    ```

    **2. Fix executive dashboard _get_active_batches()** in `executive_dashboard.py`:
    - Line 376: Change `frappe.db.count("Student Batch Name", {"status": "Active"})` to `frappe.db.count("Student Batch Name")` (Student Batch Name has no status field -- just count all batches as a fallback).
    - Keep the `frappe.db.exists("DocType", "Student Batch Name")` guard on line 375.
    - Keep the mock fallback `return 45` on line 377.

    **3. Add library_fine_per_day to University Settings** if not already present:
    - Read the university_settings.json doctype definition.
    - If `library_fine_per_day` field is not present, add it as a Float field with label "Library Fine Per Day", default "5", in a "Library Settings" section. This fixes the Overdue Books report which calls `frappe.db.get_single_value("University Settings", "library_fine_per_day")`.
    - After adding the field, run `bench --site university.local migrate` again to apply.
  </action>
  <verify>
    <automated>cd /workspace/development/frappe-bench && bench --site university.local execute university_erp.scripts.migrate_custom_fields.run 2>&1 | tail -5 && bench --site university.local console <<< "print('custom_is_faculty exists:', bool(frappe.db.sql(\"SHOW COLUMNS FROM \`tabEmployee\` LIKE 'custom_is_faculty'\")))" 2>&1 | grep "custom_is_faculty exists"</automated>
  </verify>
  <acceptance_criteria>
    - migrate_custom_fields.py contains `def run():`
    - migrate_custom_fields.py contains `employee_custom_fields.json`
    - migrate_custom_fields.py contains `leave_application_custom_fields.json`
    - migrate_custom_fields.py contains `frappe.new_doc("Custom Field")`
    - Running `SHOW COLUMNS FROM tabEmployee LIKE 'custom_is_faculty'` returns a row (column exists in DB)
    - Running `SHOW COLUMNS FROM tabEmployee LIKE 'custom_employee_category'` returns a row
    - Running `SHOW COLUMNS FROM 'tabLeave Application' LIKE 'custom_total_classes_affected'` returns a row
    - executive_dashboard.py line calling `_get_active_batches` no longer filters by `{"status": "Active"}`
    - university_settings.json contains `library_fine_per_day` field definition
  </acceptance_criteria>
  <done>All 13 Employee and 9 Leave Application custom fields exist as DB columns. Executive dashboard active batch query does not filter by non-existent status field. University Settings has library_fine_per_day field.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all 14 failing reports and re-run both audit scripts</name>
  <files>
    frappe-bench/apps/university_erp/university_erp/university_analytics/report/student_performance_analysis/student_performance_analysis.py,
    frappe-bench/apps/university_erp/university_erp/university_feedback/report/feedback_analysis/feedback_analysis.py,
    frappe-bench/apps/university_erp/university_erp/university_grievance/report/sla_compliance_report/sla_compliance_report.py,
    frappe-bench/apps/university_erp/university_erp/university_payments/report/refund_report/refund_report.py,
    frappe-bench/apps/university_erp/university_erp/university_placement/report/placement_trend/placement_trend.py,
    frappe-bench/apps/university_erp/university_erp/university_placement/report/company_wise_placement/company_wise_placement.py,
    frappe-bench/apps/university_erp/university_erp/university_placement/report/program_wise_placement/program_wise_placement.py,
    frappe-bench/apps/university_erp/university_erp/university_examinations/report/examination_result_analysis/examination_result_analysis.json,
    frappe-bench/apps/university_erp/university_erp/university_finance/report/program_wise_fee_report/program_wise_fee_report.json,
    frappe-bench/apps/university_erp/university_erp/university_library/report/overdue_books/overdue_books.py
  </files>
  <read_first>
    frappe-bench/apps/university_erp/university_erp/university_analytics/report/student_performance_analysis/student_performance_analysis.py,
    frappe-bench/apps/university_erp/university_erp/university_feedback/report/feedback_analysis/feedback_analysis.py,
    frappe-bench/apps/university_erp/university_erp/university_grievance/report/sla_compliance_report/sla_compliance_report.py,
    frappe-bench/apps/university_erp/university_erp/university_payments/report/refund_report/refund_report.py,
    frappe-bench/apps/university_erp/university_erp/university_placement/report/placement_trend/placement_trend.py,
    frappe-bench/apps/university_erp/university_erp/university_placement/report/company_wise_placement/company_wise_placement.py,
    frappe-bench/apps/university_erp/university_erp/university_placement/report/program_wise_placement/program_wise_placement.py,
    frappe-bench/apps/university_erp/university_erp/university_examinations/report/examination_result_analysis/examination_result_analysis.py,
    frappe-bench/apps/university_erp/university_erp/university_examinations/report/examination_result_analysis/examination_result_analysis.json,
    frappe-bench/apps/university_erp/university_erp/university_finance/report/program_wise_fee_report/program_wise_fee_report.py,
    frappe-bench/apps/university_erp/university_erp/university_finance/report/program_wise_fee_report/program_wise_fee_report.json,
    frappe-bench/apps/university_erp/university_erp/university_library/report/overdue_books/overdue_books.py,
    frappe-bench/apps/university_erp/university_erp/modules.txt,
    .planning/phases/03.1-cross-app-integration-audit-accessibility-testing/REPORT_AUDIT.md
  </read_first>
  <action>
    **IMPORTANT: Before making any code changes, verify Category A reports first.**

    **Step 0 -- Category A verification gate:**
    Run the report audit script to confirm the 4 faculty reports now pass after Task 1's custom field migration:
    ```bash
    cd /workspace/development/frappe-bench
    bench --site university.local execute university_erp.scripts.audit_reports.run 2>&1 | grep -E "(Faculty Directory|Department HR Summary|Faculty Workload Summary|Leave Utilization)"
    ```
    - If all 4 show PASS: proceed to Category B fixes below.
    - If any show FAIL: investigate why the custom field migration didn't resolve them. Check the specific error message. If the error is still "Unknown column custom_is_faculty", re-run Task 1's migration and bench migrate. Do NOT proceed to Category B until Category A is resolved, since the same custom field pattern may affect other reports.

    Fix each of the 14 failing reports. After Task 1 completes (custom fields migrated), 4 faculty reports should auto-fix. The remaining 10 need code changes:

    **Category A: Reports that now work after custom field migration (verify only, no code changes needed):**
    1. **Faculty Directory** -- uses `_has_custom_field("Employee", "custom_is_faculty")` and `_has_custom_field("Employee", "custom_employee_category")`. After migration, these return True and the actual DB columns exist. Should now PASS.
    2. **Department HR Summary** -- same pattern as Faculty Directory. Should now PASS.
    3. **Faculty Workload Summary** -- uses `_has_custom_field("Employee", "custom_is_faculty")`. Should now PASS.
    4. **Leave Utilization Report** -- uses `_has_custom_field("Leave Application", "custom_total_classes_affected")`. Should now PASS.

    **Category B: Reports needing SQL column fixes (code changes required):**

    5. **Student Performance Analysis** (`student_performance_analysis.py`):
       - Error: `Unknown column 's.program'` -- but code actually uses `s.custom_program`. The Student doctype may not have `custom_program` field either.
       - Fix: Add a `_has_custom_field("Student", "custom_program")` guard. If field doesn't exist, use `NULL as program` instead of `s.custom_program as program`. Also fix the filter condition on line 95 to be guarded the same way.
       - Also fix the f-string SQL on line 106 to use parameterized query with `%(where_clause)s` pattern, or at minimum keep the existing pattern but ensure it works.

    6. **Feedback Analysis** (`feedback_analysis.py`):
       - Error: `Unknown column 'ff.title'` -- but code uses `ff.form_title`. The Feedback Form doctype likely doesn't have `form_title`.
       - Fix: Read the Feedback Form doctype JSON to find the actual field names. Replace `ff.form_title` with the correct field (likely `ff.title` or `ff.name`). If no title-like field exists, use `ff.name as form_title`.
       - Also fix SQL injection: lines 138-146 use f-string interpolation for filter values. Convert to parameterized queries using `%(filter_name)s` placeholders.

    7. **SLA Compliance Report** (`sla_compliance_report.py`):
       - Error: `Unknown column 'g.submission_date'` -- but code uses `g.creation` not `g.submission_date`. The error is from a different version. Verify the current code does NOT reference `g.submission_date`. If the current code already uses `g.creation`, the report may already be fixed. Re-run to check.
       - If still failing, check what columns the Grievance doctype actually has and fix the SQL accordingly.
       - Also fix SQL injection: lines 127-137 use f-string interpolation. Convert to parameterized queries.

    8. **Refund Report** (`refund_report.py`):
       - Error: `Unknown column 'fr.reason'` -- code on line 116 uses `fr.refund_reason as reason`. The Fee Refund doctype may not have `refund_reason`.
       - Fix: Read the Fee Refund doctype JSON to find actual field names. If `refund_reason` doesn't exist, check for `reason`, `refund_remarks`, or similar. If no such field, use `'' as reason`.
       - Also: line 127 uses `.format(conditions=conditions)` which is safe since conditions come from internal code, but verify.

    9. **Placement Trend** (`placement_trend.py`):
       - Error: `Unknown column 'pjo.package_offered'` -- but current code uses `pa.offered_ctc` (Placement Application has this field). This error may be from a different version.
       - Fix: Verify current code doesn't reference `pjo.package_offered`. If it does reference `pd.package_offered` (Placement Drive has this field per JSON), that's fine since the column exists. If it references `pjo` (Placement Job Opening alias), change to `pd.package_offered` or `pa.offered_ctc`.

    10. **Company Wise Placement** (`company_wise_placement.py`):
        - Same pattern as Placement Trend. Error says `pjo.package_offered`. Current code uses `pa.offered_ctc`.
        - Fix: Same approach -- verify and fix any remaining `pjo.package_offered` references.

    11. **Program Wise Placement** (`program_wise_placement.py`):
        - Error: `Unknown column 'pa.program'` -- current code uses `s.custom_program` via `_has_custom_field` guard.
        - Fix: The `_has_custom_field` guard should handle this. Verify the Student doctype has (or doesn't have) `custom_program`. If it doesn't and the guard works correctly, the report should use NULL. If the guard returns True but column doesn't exist (same pattern as faculty reports before migration), need to add the custom_program field to Student or fix the guard.

    **Category C: Reports with broken module import paths:**

    12. **Examination Result Analysis** (`examination_result_analysis.json`):
        - Error: `No module named 'university_erp.university_examinations.report.examination_result_analysis.e'` (truncated)
        - The report JSON has `"module": "University Examinations"` which maps to `university_examinations`. The Python file is at `university_erp/university_examinations/report/examination_result_analysis/examination_result_analysis.py`.
        - Frappe resolves: `university_erp.university_examinations.report.examination_result_analysis.examination_result_analysis`.
        - Fix: Ensure the report JSON `module` field is exactly `"University Examinations"` (matching modules.txt). Also ensure there's an `__init__.py` in `university_erp/university_examinations/report/examination_result_analysis/`. If `__init__.py` is missing, create an empty one.

    13. **Program-wise Fee Report** (`program_wise_fee_report.json`):
        - Error: `No module named 'university_erp.university_finance.report.program_wise_fee_report'`
        - Same pattern. JSON has `"module": "University Finance"`, file is at `university_finance/report/program_wise_fee_report/`.
        - Fix: Ensure `__init__.py` exists in the report directory. Check if `university_finance/report/__init__.py` also exists.

    **Category D: Missing settings field:**

    14. **Overdue Books** (`overdue_books.py`):
        - Error: `Field library_fine_per_day does not exist on University Settings`
        - Already fixed in Task 1 by adding the field to University Settings. Verify it works after Task 1.

    **After all fixes, re-run both audit scripts:**
    ```bash
    cd /workspace/development/frappe-bench
    bench --site university.local execute university_erp.scripts.audit_reports.run
    bench --site university.local execute university_erp.scripts.audit_faculty_api.run
    ```

    Copy the updated output files (REPORT_AUDIT.md and API_AUDIT.md) to the phase directory, replacing the old versions.
  </action>
  <verify>
    <automated>cd /workspace/development/frappe-bench && bench --site university.local execute university_erp.scripts.audit_reports.run 2>&1 | grep -E "PASS|FAIL|ERROR" | head -20 && bench --site university.local execute university_erp.scripts.audit_faculty_api.run 2>&1 | grep -E "PASS|FAIL|ERROR" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Re-running audit_reports.py shows "Faculty Directory" as PASS (not FAIL)
    - Re-running audit_reports.py shows "Faculty Workload Summary" as PASS
    - Re-running audit_reports.py shows "Department HR Summary" as PASS
    - Re-running audit_reports.py shows "Leave Utilization Report" as PASS
    - Re-running audit_reports.py shows "Student Performance Analysis" as PASS (or graceful empty result, not SQL error)
    - Re-running audit_reports.py shows "Feedback Analysis" as PASS
    - Re-running audit_reports.py shows "SLA Compliance Report" as PASS
    - Re-running audit_reports.py shows "Refund Report" as PASS
    - Re-running audit_reports.py shows "Placement Trend" as PASS
    - Re-running audit_reports.py shows "Company Wise Placement" as PASS
    - Re-running audit_reports.py shows "Program Wise Placement" as PASS
    - Re-running audit_reports.py shows "Examination Result Analysis" as PASS
    - Re-running audit_reports.py shows "Program-wise Fee Report" as PASS
    - Re-running audit_reports.py shows "Overdue Books" as PASS
    - Re-running audit_faculty_api.py shows at least 20/27 endpoints as PASS (some may fail due to missing test data, not schema errors)
    - Updated REPORT_AUDIT.md exists in the phase directory
    - Updated API_AUDIT.md exists in the phase directory
    - No report fails with "Unknown column" SQL error (1054)
    - No report fails with "No module named" import error
  </acceptance_criteria>
  <done>All 14 previously-failing university_erp reports execute without SQL or import errors. All 27 faculty API endpoints respond without MySQL 1054 "Unknown column custom_is_faculty" error. Updated audit reports confirm the fixes.</done>
</task>

</tasks>

<verification>
1. Custom field migration: `bench --site university.local console <<< "print(bool(frappe.db.sql(\"SHOW COLUMNS FROM \`tabEmployee\` LIKE 'custom_is_faculty'\")))"` returns True
2. Category A gate: All 4 faculty reports (Faculty Directory, Department HR Summary, Faculty Workload Summary, Leave Utilization Report) show PASS in audit_reports.py output before proceeding to Category B fixes
3. Report audit: Re-run `bench --site university.local execute university_erp.scripts.audit_reports.run` -- all 14 previously-failing reports now PASS
4. Faculty API audit: Re-run `bench --site university.local execute university_erp.scripts.audit_faculty_api.run` -- endpoints no longer fail with MySQL 1054
5. Executive dashboard: `bench --site university.local execute university_erp.university_erp.analytics.dashboards.executive_dashboard.get_dashboard` returns dict without error
</verification>

<success_criteria>
- 13 Employee custom fields exist as DB columns on tabEmployee
- 9 Leave Application custom fields exist as DB columns on tabLeave Application
- 0 university_erp reports fail with "Unknown column" SQL errors
- 0 university_erp reports fail with "No module named" import errors
- Faculty API endpoints return data (not MySQL 1054 errors)
- Executive dashboard returns metrics without error
- Updated REPORT_AUDIT.md and API_AUDIT.md in phase directory
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cross-app-integration-audit-accessibility-testing/03.1-04-SUMMARY.md`
</output>
