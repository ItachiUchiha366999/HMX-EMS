---
phase: 03.4-post-fork-backend-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frappe-bench/apps/university_erp/university_erp/setup/seed_demo_v2.py
  - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md
autonomous: true
gap_closure: false
requirements:
  - AUDIT-BACKEND-01
must_haves:
  truths:
    - "After clean_all + seed_all, tabStudent has >= 500 rows"
    - "After seed, tabEmployee with custom_is_faculty=1 has >= 50 rows"
    - "After seed, tabGL Entry has >= 800 rows posted via university_finance"
    - "Re-seed is idempotent: second run produces same counts (no duplicates)"
    - "Fiscal Year 2026-2027 exists before any fee posting"
    - "Seeded fixtures contain zero references to 'education.education' module path"
    - "Wipe scope filtered to university_erp modules ONLY: 0 rows wiped from DocTypes whose module='Education' (per CONTEXT D-08)"
    - "Reseed scope filtered to university_erp modules ONLY: 0 rows inserted into DocTypes whose module='Education' (per CONTEXT D-08 — defensive assertion)"
    - "UNIVERSITY_ERP_MODULES whitelist used for both wipe and reseed; produced and logged to AUDIT_REPORT for review"
  artifacts:
    - path: "frappe-bench/apps/university_erp/university_erp/setup/seed_demo_v2.py"
      provides: "Wipe + seed orchestrator with FY auto-create + GL backfill"
    - path: ".planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md"
      provides: "Consolidated demo-data validation report (counts + idempotency)"
  key_links:
    - from: "seed_demo_v2.seed_all"
      to: "university_finance.GLPostingManager"
      via: "Fees.submit() trigger"
      pattern: "GLPostingManager"
---

<objective>
Wipe existing pre-3.3.1 demo data and re-seed against post-fork schema (D-08).
Demo data is the prerequisite for plans 02-05 — they all depend on populated data.

Purpose: Validate that the post-Education-fork schema (71 doctypes redistributed
across 7 university_erp modules) accepts a clean wipe-and-reseed without FK
errors, fixture drift, or fiscal-year gaps. Produces 500+ students, 50+ faculty,
800+ GL entries — the baseline plans 02-05 audit against.

Output: Validated demo data in DB + `03.4-01-AUDIT_REPORT.md` (single consolidated
report per D-12) with counts, idempotency check, and pass/fail status.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-post-fork-backend-audit-fix/03.4-CONTEXT.md
@.planning/phases/03.4-post-fork-backend-audit-fix/03.4-RESEARCH.md
@.planning/phases/03.3.1-fork-education-app-into-university-erp-audit-each-doctype-fields-features-and-workflows-then-fork-into-custom-app/DOCTYPE_MAPPING.md
@frappe-bench/apps/university_erp/university_erp/setup/seed_demo_v2.py
@frappe-bench/apps/university_erp/university_erp/setup/seed_comprehensive_demo_data.py
@frappe-bench/apps/university_erp/university_erp/modules.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — fixture grep + DB backup verify</name>
  <files>
    .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md
  </files>
  <read_first>
    - frappe-bench/apps/university_erp/university_erp/fixtures/ (list all .json)
    - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-CONTEXT.md (D-08 backup path)
    - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-RESEARCH.md (Pitfall 5)
  </read_first>
  <action>
    1. Verify backup exists at `development/backups/pre-03.3.1-20260514-181929/` (rollback target per D-08). Run `ls -la development/backups/pre-03.3.1-20260514-181929/` and capture output.
    2. Grep all fixtures for stale Education refs (Pitfall 5 from RESEARCH.md):
       `grep -RIn "education.education" frappe-bench/apps/university_erp/university_erp/fixtures/ || echo "CLEAN"`
       Expected: "CLEAN" (Phase 03.3.1-06 should have eliminated all such refs).
    3. Capture pre-wipe row counts via:
       `cd frappe-bench && bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Student"}'`
       Repeat for Employee, GL Entry, Fees, Program Enrollment.
    4. **Derive the UNIVERSITY_ERP_MODULES whitelist from DB (per CONTEXT D-08 wipe scope):**
       Read `frappe-bench/apps/university_erp/university_erp/modules.txt` to get the canonical 17-module list. Then run:
       ```
       cd frappe-bench && bench --site university.local execute frappe.client.get_list --kwargs '{"doctype":"DocType","filters":{"module":["in",["University ERP","University Academics","University Student Info","University Admissions","University Hostel","University Examinations","University OBE","University LMS","University Library","University Research","University Placement","University Transport","University Inventory","University Payments","University Integrations","University Analytics","Faculty Management","University Finance"]]},"fields":["name","module"],"limit_page_length":0}'
       ```
       Capture the count of returned doctypes (expect ~250-300). Persist the JSON output to AUDIT_REPORT.md `## Whitelist Derived` section.
    5. **Verify Education-module DocTypes are NOT in the whitelist (defensive check):**
       Run:
       ```
       cd frappe-bench && bench --site university.local execute frappe.client.get_list --kwargs '{"doctype":"DocType","filters":{"module":"Education"},"fields":["name"]}'
       ```
       Expect 7 DocTypes (Quiz, Question, Quiz Activity, Quiz Result, Article, Options, Student Report Generation Tool). Verify NONE appear in the whitelist from step 4. If any overlap → ABORT (means the module-ownership data is wrong).
    6. Initialize AUDIT_REPORT.md at `.planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md` with sections:
       `# Phase 03.4 Plan 01 — Demo Data Validate & Re-seed`
       `## Pre-flight` (backup status, fixture grep result, pre-wipe counts)
       `## Whitelist Derived` (UNIVERSITY_ERP_MODULES + count of doctypes per D-08 wipe scope)
       `## Education Boundary Check` (the 7 ARCHIVE_DOCTYPES — confirms NONE in whitelist)
       `## Wipe Phase` (filled by Task 2 — must include "Wipe scope: N doctypes from university_erp modules; 0 from Education")
       `## Seed Phase` (filled by Task 2 — must include "Seed target: N doctypes; 0 attempts to write to module=Education")
       `## Idempotency Check` (filled by Task 3)
       `## Final Counts vs Baseline` (filled by Task 3)
       `## Status: PASS|FAIL` (filled by Task 3)
  </action>
  <verify>
    <automated>test -d development/backups/pre-03.3.1-20260514-181929 && grep -L "education.education" frappe-bench/apps/university_erp/university_erp/fixtures/*.json 2>/dev/null && test -f .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls development/backups/pre-03.3.1-20260514-181929/` exits 0 (backup present).
    - `grep -RIn "education.education" frappe-bench/apps/university_erp/university_erp/fixtures/` returns 0 matches OR exits non-zero (no matches).
    - File exists: `.planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md`.
    - Report contains the heading `## Pre-flight` (verify with `grep "## Pre-flight" .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md`).
    - Report records 5 pre-wipe row counts (Student, Employee, GL Entry, Fees, Program Enrollment).
    - Report contains `## Whitelist Derived` section listing all 18 university_erp modules and the resulting DocType count.
    - Report contains `## Education Boundary Check` section confirming the 7 ARCHIVE_DOCTYPES (Quiz, Question, Quiz Activity, Quiz Result, Article, Options, Student Report Generation Tool) are present in module='Education' but NOT in the whitelist (`grep -A 12 "Education Boundary Check"` shows the 7 names AND the line `Overlap with whitelist: 0`).
  </acceptance_criteria>
  <done>
    Backup verified, fixtures clean of Education refs, pre-wipe counts captured, UNIVERSITY_ERP_MODULES whitelist derived from DB and confirmed disjoint from module='Education', AUDIT_REPORT scaffold exists with all sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wipe + reseed against post-3.3.1 schema</name>
  <files>
    frappe-bench/apps/university_erp/university_erp/setup/seed_demo_v2.py
    .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md
  </files>
  <read_first>
    - frappe-bench/apps/university_erp/university_erp/setup/seed_demo_v2.py (entire file — clean_all + seed_all)
    - frappe-bench/apps/university_erp/university_erp/setup/seed_comprehensive_demo_data.py (lines 209-326 — clean_all FK ordering)
    - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-RESEARCH.md (## Demo Data Wipe-and-Reseed Strategy + ## Re-seed gotchas)
    - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    1. **Pre-wipe gate (CONTEXT D-08 module-ownership filter):**
       Read the current `seed_demo_v2.clean_all` source via `head -200 frappe-bench/apps/university_erp/university_erp/setup/seed_demo_v2.py` and confirm every doctype it targets is in the UNIVERSITY_ERP_MODULES whitelist from Task 1. If any target doctype's `module` is `'Education'` or anything outside the whitelist → STOP and patch `clean_all` to filter via:
       ```python
       UNIVERSITY_ERP_MODULES = ["University ERP","University Academics","University Student Info","University Admissions","University Hostel","University Examinations","University OBE","University LMS","University Library","University Research","University Placement","University Transport","University Inventory","University Payments","University Integrations","University Analytics","Faculty Management","University Finance"]
       def _is_in_scope(doctype):
           m = frappe.db.get_value("DocType", doctype, "module")
           return m in UNIVERSITY_ERP_MODULES
       # In every wipe step:
       if not _is_in_scope(doctype):
           print(f"SKIP wipe (out of scope): {doctype} module={frappe.db.get_value('DocType', doctype, 'module')}")
           continue
       ```
       Commit the patch as a separate atomic commit before executing wipe.
    2. Execute wipe (FK-safe ordering already correct per RESEARCH.md):
       `cd frappe-bench && bench --site university.local execute university_erp.setup.seed_demo_v2.clean_all`
       Capture stdout/stderr → AUDIT_REPORT.md `## Wipe Phase`. The captured output must include "SKIP wipe (out of scope)" lines if `clean_all` ever encounters a non-whitelisted target (or zero such lines if the script is already correctly scoped — both are acceptable; what matters is the assertion below).
    3. **Wipe scope verification (post-wipe DB query):**
       Confirm Education-module DocType row counts are unchanged (no collateral damage):
       ```
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Quiz"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Article"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Question"}'
       ```
       (Pre-wipe these were 0 per the 2026-05-15 DB recon; if any returns > 0 it means data appeared during the wipe — ABORT.)
    4. Smoke-check post-wipe counts on whitelisted DocTypes (must all be 0 or near-0):
       For each doctype in [Student, Fees, GL Entry, Program Enrollment, Course Enrollment]:
       `bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Student"}'`
       Append counts to `## Wipe Phase`. If Student count > 0 → ABORT and document.
    5. Verify Fiscal Year 2026-2027 exists (carryover from April re-run per RESEARCH.md):
       `bench --site university.local execute frappe.client.get_value --kwargs '{"doctype":"Fiscal Year","filters":{"name":"2026-2027"}}'`
       If missing, ensure `seed_demo_v2.seed_all` creates it (the script's `_seed_core` already handles this — verify by reading seed_demo_v2.py for `2026-2027` literal). If absent from script, add idempotent creation block:
       ```python
       if not frappe.db.exists("Fiscal Year", "2026-2027"):
           frappe.get_doc({"doctype":"Fiscal Year","year":"2026-2027","year_start_date":"2026-04-01","year_end_date":"2027-03-31"}).insert(ignore_permissions=True)
       ```
    6. **Pre-reseed gate (CONTEXT D-08 reseed scope):**
       Read the current `seed_demo_v2.seed_all` source and confirm every `frappe.get_doc({"doctype": <X>, ...})` and every direct insert references a DocType in the UNIVERSITY_ERP_MODULES whitelist. If any line targets a DocType whose `module` is `'Education'` (or anything outside the whitelist), patch the seed function to wrap inserts with the same `_is_in_scope()` guard from step 1 — but as a HARD ASSERT (FAIL LOUDLY) instead of skip:
       ```python
       def _assert_in_scope(doctype):
           m = frappe.db.get_value("DocType", doctype, "module")
           if m not in UNIVERSITY_ERP_MODULES:
               raise Exception(f"D-08 violation: refused to seed {doctype} (module={m}) — reseed scope is university_erp ONLY")
       # Before every frappe.get_doc({"doctype": X, ...}).insert() in seed_*:
       _assert_in_scope(X)
       ```
       Commit the patch as a separate atomic commit before executing reseed.
    7. Run reseed:
       `cd frappe-bench && bench --site university.local execute university_erp.setup.seed_demo_v2.seed_all`
       Capture full output → AUDIT_REPORT.md `## Seed Phase`. The captured output must NOT contain any "D-08 violation" exception message.
    8. **Reseed scope verification (post-seed DB query):**
       Confirm Education-module DocType row counts are still 0 (no seed leakage):
       ```
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Quiz"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Article"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Question"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Quiz Activity"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Quiz Result"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Options"}'
       bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Student Report Generation Tool"}'
       ```
       Each MUST return 0. If any returns > 0 → reseed leaked into Education-module doctype → ABORT and document.
    9. If any "Date X is not in any active Fiscal Year" error appears, fix by ensuring step 5 ran BEFORE seed_all. If GL backfill is required (Fees in docstatus=1 with no GL entries — RESEARCH.md gotcha), confirm `seed_demo_v2.py` GL backfill phase ran (search log for "GL backfill" or "_backfill_gl_entries"). If missing, the script's existing backfill block must execute; do NOT add new backfill logic — reuse existing per RESEARCH.md "Don't Hand-Roll".
  </action>
  <verify>
    <automated>cd frappe-bench && bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Student"}' | grep -E '5[0-9]{2}|[6-9][0-9]{2}|[0-9]{4,}'</automated>
  </verify>
  <acceptance_criteria>
    - Post-wipe Student count == 0 (logged in `## Wipe Phase`).
    - Post-seed Student count >= 500 (verifiable via the bench get_count command above).
    - Post-seed Employee count where custom_is_faculty=1 >= 50 (verify: `bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Employee","filters":{"custom_is_faculty":1}}'`).
    - Post-seed GL Entry count >= 800 (verify: `bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"GL Entry"}'`).
    - Fiscal Year "2026-2027" exists (`bench ... get_value` returns non-null).
    - AUDIT_REPORT.md `## Wipe Phase` and `## Seed Phase` sections both populated with command output.
    - `verify_education_removable.py` still exits 0: `cd frappe-bench && bench --site university.local execute university_erp.scripts.verify_education_removable.run` — exit code 0.
    - **D-08 wipe boundary:** Post-wipe row counts for ALL 7 Education-module DocTypes (Quiz, Article, Question, Quiz Activity, Quiz Result, Options, Student Report Generation Tool) remain at 0 (verified via the 7 `bench get_count` calls in step 3).
    - **D-08 reseed boundary:** Post-reseed row counts for the same 7 Education-module DocTypes remain at 0 (verified in step 8). Seed log contains zero "D-08 violation" exception lines (`grep -c "D-08 violation" AUDIT_REPORT.md` returns 0).
    - **D-08 scoping commits:** If `seed_demo_v2.py` required patches in steps 1 or 6, the patches landed as separate atomic commits (`git log --oneline | grep -E "feat\(03\.4-01\): apply UNIVERSITY_ERP_MODULES (wipe|reseed) scope"` returns ≥1 line).
  </acceptance_criteria>
  <done>
    DB wiped clean of demo data and reseeded with 500+ students, 50+ faculty, 800+ GL entries, FY 2026-2027 present. Wipe and reseed both bounded to UNIVERSITY_ERP_MODULES whitelist (D-08); 7 Education-module DocTypes remain at 0 rows throughout. All logged in AUDIT_REPORT.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Idempotency check + final report</name>
  <files>
    .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md
  </files>
  <read_first>
    - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md (current state from Task 2)
    - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-CONTEXT.md (D-09 idempotent script requirement)
  </read_first>
  <action>
    1. Capture post-seed counts for: Student, Employee (custom_is_faculty=1), GL Entry, Fees, Program Enrollment, Course Enrollment, Hostel Allocation, Teaching Assignment.
    2. Re-run `bench --site university.local execute university_erp.setup.seed_demo_v2.seed_all` WITHOUT a wipe (idempotency test).
    3. Capture counts a second time.
    4. Diff: counts MUST match exactly (idempotency per D-09). If any count grows → script is not idempotent → mark FAIL and identify which `_seed_*` function lacks `frappe.db.exists` check.
    5. Append `## Idempotency Check` section with table:
       | DocType | Count Run 1 | Count Run 2 | Delta | Status |
    6. Append `## Final Counts vs Baseline` section:
       | DocType | Required | Actual | Status |
       | Student | >= 500 | NNN | PASS/FAIL |
       | Employee (faculty) | >= 50 | NN | PASS/FAIL |
       | GL Entry | >= 800 | NNN | PASS/FAIL |
       | Fiscal Year 2026-2027 | exists | exists | PASS/FAIL |
    7. Append `## Status: PASS` (or FAIL with remediation list).
  </action>
  <verify>
    <automated>grep -E "^## Status: PASS" .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT_REPORT.md contains heading `## Idempotency Check` (grep `^## Idempotency Check`).
    - AUDIT_REPORT.md contains heading `## Final Counts vs Baseline` (grep).
    - AUDIT_REPORT.md ends with line matching `^## Status: PASS$`.
    - Idempotency table shows all deltas == 0.
    - Final counts table shows Student >= 500, Employee(faculty) >= 50, GL Entry >= 800, FY exists.
  </acceptance_criteria>
  <done>
    Single consolidated AUDIT_REPORT.md (per D-12) shows demo data is post-fork-clean, idempotent, and meets D-09 volume requirements.
  </done>
</task>

</tasks>

<verification>
- `cd frappe-bench && bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"Student"}'` returns >= 500
- `cd frappe-bench && bench --site university.local execute frappe.client.get_count --kwargs '{"doctype":"GL Entry"}'` returns >= 800
- `grep "^## Status: PASS$" .planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md` exits 0
- `cd frappe-bench && bench --site university.local execute university_erp.scripts.verify_education_removable.run` exits 0
</verification>

<success_criteria>
1. Pre-3.3.1 demo data is fully wiped and replaced with post-fork-schema demo data.
2. Counts meet D-09: 500+ students, 50+ faculty, 800+ GL entries.
3. Re-running seed_all is a no-op (idempotency confirmed).
4. Single consolidated `03.4-01-AUDIT_REPORT.md` documents all phases with PASS status.
5. Plans 02-05 can now run against this baseline data.
</success_criteria>

<must_haves>
truths:
  - "After clean_all + seed_all, tabStudent has >= 500 rows"
  - "After seed, tabEmployee with custom_is_faculty=1 has >= 50 rows"
  - "After seed, tabGL Entry has >= 800 rows posted via university_finance"
  - "Re-seed is idempotent: second run produces same counts"
  - "Fiscal Year 2026-2027 exists before any fee posting"
artifacts:
  - path: "frappe-bench/apps/university_erp/university_erp/setup/seed_demo_v2.py"
    provides: "Wipe + seed orchestrator (existing, possibly minor FY guard added)"
  - path: ".planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-AUDIT_REPORT.md"
    provides: "Consolidated demo-data validation report"
key_links:
  - from: "seed_demo_v2.seed_all"
    to: "university_finance GLPostingManager"
    via: "Fees.submit() trigger creates GL Entries"
    pattern: "GLPostingManager"
</must_haves>

<output>
After completion, create `.planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-SUMMARY.md`
</output>
