---
phase: 03.4-post-fork-backend-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frappe-bench/apps/university_erp/university_erp/setup/seed_comprehensive_demo_data.py
autonomous: true
requirements: [AUDIT-BACKEND-04]

must_haves:
  truths:
    - "Frappe Desk list views for Student, Employee, Program, Course, Fees, Hostel Allocation, Library Article show 50+ records each"
    - "GL Entries exist for fee records created via university_finance module"
    - "University Accounts Settings singleton is configured with valid account mappings"
    - "20+ demo users exist across all 9 roles (Administrator, Student, Faculty, HOD, Parent, VC, Registrar, Dean, Finance Officer)"
  artifacts:
    - path: "frappe-bench/apps/university_erp/university_erp/setup/seed_comprehensive_demo_data.py"
      provides: "Comprehensive demo data seeder with 500+ students, 50+ faculty, transactional data"
      min_lines: 800
  key_links:
    - from: "seed_comprehensive_demo_data.py"
      to: "university_finance module"
      via: "Fee records that post GL entries"
      pattern: "Fees.*submit|make_gl_entries"
    - from: "seed_comprehensive_demo_data.py"
      to: "University Accounts Settings"
      via: "Configure accounts before fee creation"
      pattern: "University Accounts Settings"
---

<objective>
Seed comprehensive demo data into the university ERP so every Frappe Desk list view shows meaningful content and financial data validates the Phase 03.3 fork end-to-end.

Purpose: Demo data is the prerequisite for ALL subsequent audit tasks (workflow testing, report execution, permission verification all need data to work against). Financial transactions through university_finance validate the accounts fork actually works with real data volumes.

Output: New `seed_comprehensive_demo_data.py` script creating 500+ students, 50+ faculty, full transactional data across all modules, and 20+ role-specific demo users.
</objective>

<execution_context>
@/workspace/development/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/development/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-post-fork-backend-audit-fix/03.4-CONTEXT.md
@.planning/phases/03.4-post-fork-backend-audit-fix/03.4-RESEARCH.md
@.planning/phases/03.3-erpnext-accounts-fork/03.3-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive demo data seed script</name>
  <files>frappe-bench/apps/university_erp/university_erp/setup/seed_comprehensive_demo_data.py</files>
  <read_first>
    - frappe-bench/apps/university_erp/university_erp/setup/seed_demo_data.py (existing seed script -- study clean_all() and creation patterns)
    - frappe-bench/apps/university_erp/university_erp/modules.txt (all 21 modules to seed data for)
    - frappe-bench/apps/university_erp/university_erp/hooks.py (override_doctype_class for correct class usage)
    - frappe-bench/apps/university_erp/university_erp/university_payments/doctype/university_accounts_settings/university_accounts_settings.json (settings singleton field structure)
    - frappe-bench/apps/university_erp/university_erp/university_finance/general_ledger.py (GL posting entry point)
    - .planning/phases/03.4-post-fork-backend-audit-fix/03.4-RESEARCH.md (demo data architecture section, existing vs required counts table, pitfall section)
  </read_first>
  <action>
Create a new file `seed_comprehensive_demo_data.py` in `university_erp/setup/`. This is a NEW file (per D-05: Claude decides; new file avoids conflicts with existing 1148-line script). Structure:

**Entry points:**
- `seed_all()` -- orchestrator that runs all phases in order
- `clean_all()` -- wipe existing data (extend pattern from existing seed_demo_data.py clean_all(), add university_finance doctypes: GL Entry, Journal Entry, Payment Entry, Payment Ledger Entry)

**CRITICAL: Creation order (dependency chain):**
1. `_seed_core()` -- Company (verify "National Institute of Technology" exists), Academic Year "2025-2026", Academic Term "Semester 1" and "Semester 2", Fiscal Year "2025-2026", Chart of Accounts accounts (Fee Collection, Fee Income, Student Receivable, Cash, Bank)
2. `_seed_accounts_settings()` -- Configure University Accounts Settings singleton: company="National Institute of Technology", auto_post_gl=1, fee_collection_account, fee_income_account, fee_receivable_account (use actual Account names created in step 1)
3. `_seed_departments()` -- 8 University Departments: Computer Science, Electronics, Mechanical, Civil, Electrical, Chemical, Mathematics, Physics
4. `_seed_programs_courses()` -- 8 Programs (B.Tech CSE, B.Tech ECE, B.Tech ME, B.Tech CE, B.Tech EE, M.Tech CSE, M.Tech ECE, MBA), 5-6 courses per program (40+ total), Course Prerequisites, Fee Structures per program
5. `_seed_faculty()` -- 50+ Employees with custom_is_faculty=1 and custom_employee_category="Teaching", Faculty Profiles linked, Instructors created, Teaching Assignments
6. `_seed_students()` -- 500+ Students across programs (use random Indian names from a curated list of ~50 first names and ~50 last names), Program Enrollments, Student Groups, Guardians (1 per 3 students), Course Enrollments
7. `_seed_finance()` -- Fee records for all students (use `frappe.get_doc({"doctype": "Fees", ...}).insert().submit()` to trigger GL posting through university_finance). Verify GL Entries are created by checking `frappe.db.count("GL Entry")` after submission. Also create some Payment Entries.
8. `_seed_hostel()` -- 5 Hostel Buildings, 100+ Rooms, 200+ Hostel Allocations, Hostel Mess records, Mess Menus
9. `_seed_library()` -- 50+ Library Articles, Library Members (link to students), Library Transactions (issue/return)
10. `_seed_transport()` -- 5 Transport Routes, 10 Vehicles, 100+ Transport Allocations
11. `_seed_placement()` -- 10 Placement Companies, 20 Job Openings, 50+ Placement Applications
12. `_seed_examinations()` -- Exam Schedules, Hall Tickets, Assessment Plans, Assessment Results with grades
13. `_seed_research_obe()` -- Research Publications, Research Projects, Program Outcomes, Course Outcomes, CO-PO Mappings
14. `_seed_lms()` -- LMS Courses (one per actual Course), LMS Content, LMS Assignments
15. `_seed_grievance_feedback()` -- 10 Grievances with various statuses, 5 Feedback Forms, Suggestions
16. `_seed_attendance()` -- Student Attendance records (batch create ~5000 records using frappe.db.bulk_insert or individual inserts), Notice Board entries, Emergency Alerts
17. `_seed_users()` -- Create 20+ demo users with proper role assignments:
    - admin@nit.edu (Administrator -- likely exists)
    - student1@nit.edu through student5@nit.edu (Student role)
    - faculty1@nit.edu through faculty5@nit.edu (Academics User role + custom_is_faculty)
    - hod1@nit.edu, hod2@nit.edu (Academics User + Department head link)
    - parent1@nit.edu, parent2@nit.edu (Guardian role, linked to student guardians)
    - vc@nit.edu (University VC role)
    - registrar@nit.edu (University Registrar role)
    - dean@nit.edu (University Dean role)
    - finance@nit.edu (University Finance Officer role)
    - warden@nit.edu (for hostel workflows)
    - transport@nit.edu (for transport workflows)

**Idempotency pattern (D-04):**
Every creation function MUST check `frappe.db.exists()` before creating. Example:
```python
if not frappe.db.exists("Student", {"student_name": name}):
    doc = frappe.get_doc({...}).insert(ignore_permissions=True)
```

**Key implementation details:**
- Use `frappe.flags.ignore_permissions = True` at script start
- Use `frappe.db.commit()` after each major section (not after every record)
- Print progress: `print(f"Created {count} students...")`
- For bulk Student creation, use a loop with randomized names from curated Indian name lists
- Fee submission MUST trigger GL posting. After creating fees, assert: `gl_count = frappe.db.count("GL Entry"); assert gl_count > 0, "GL posting failed"`
- Handle errors gracefully: wrap each section in try/except, print traceback, continue to next section

**Usage:**
```
bench --site university.local execute university_erp.setup.seed_comprehensive_demo_data.clean_all
bench --site university.local execute university_erp.setup.seed_comprehensive_demo_data.seed_all
```
  </action>
  <verify>
    <automated>cd /workspace/development/frappe-bench && bench --site university.local execute university_erp.setup.seed_comprehensive_demo_data.seed_all 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frappe-bench/apps/university_erp/university_erp/setup/seed_comprehensive_demo_data.py
    - File contains `def seed_all():` and `def clean_all():`
    - File contains `_seed_accounts_settings` function that configures University Accounts Settings singleton
    - File contains `_seed_finance` function that creates Fees and submits them
    - File contains idempotency checks: grep finds `frappe.db.exists` in at least 15 locations
    - File contains GL Entry verification: grep finds `GL Entry` reference
    - After execution: `bench --site university.local execute` with `frappe.db.count("Student")` returns 500+
    - After execution: `bench --site university.local execute` with `frappe.db.count("GL Entry")` returns > 0
    - After execution: `bench --site university.local execute` with `frappe.db.count("Employee", {"custom_is_faculty": 1})` returns 50+
  </acceptance_criteria>
  <done>
    500+ students, 50+ faculty, 40+ courses, 8 programs, financial transactions with GL entries, hostel/library/transport/placement/exam/research/LMS/grievance data all seeded. University Accounts Settings configured. 20+ role-specific demo users created. Every Frappe Desk list view shows meaningful content.
  </done>
</task>

</tasks>

<verification>
- `bench --site university.local execute university_erp.setup.seed_comprehensive_demo_data.seed_all` completes without fatal errors
- Student count >= 500, Employee count >= 50, GL Entry count > 0
- University Accounts Settings has auto_post_gl=1 and non-empty account fields
- Demo users for all 9 roles exist and can log in
</verification>

<success_criteria>
- Comprehensive demo data populated across all 21 university modules
- Financial flow (Fee -> GL Entry) works end-to-end through university_finance fork
- Every Frappe Desk list view for university doctypes shows 50+ records
- 20+ demo users with correct role assignments
- Script is idempotent (safe to re-run)
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-post-fork-backend-audit-fix/03.4-01-SUMMARY.md`
</output>
