---
phase: 03.3.1
plan: 02
subsystem: education-fork-academics
tags: [education-fork, university-academics, wave-2, doctype-fork, property-setter, parallel-execution]
requires:
  - 03.3.1-01 (DOCTYPE_MAPPING.md + fork_subset helper)
  - frappe-bench/apps/education/education/education/doctype/ (read-only source)
provides:
  - 29 University Academics doctypes forked under university_academics/doctype/
  - 7 archived doctypes (quiz suite + article + options) under university_academics/_archived/
  - Property Setter fixture relabeling Education Settings -> "Academics Settings"
  - Populated DOCTYPES list in university_academics/tests/test_education_fork.py
affects:
  - .planning/STATE.md, .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md (EDU-01, EDU-02, EDU-04, EDU-09)
tech-stack:
  added: []
  patterns: [fork-subset domain partitioning, Property Setter doctype relabeling, archived doctype isolation]
key-files:
  created:
    - frappe-bench/apps/university_erp/university_erp/university_academics/doctype/{29 dirs}
    - frappe-bench/apps/university_erp/university_erp/university_academics/_archived/{7 dirs}
    - frappe-bench/apps/university_erp/university_erp/university_academics/fixtures/property_setter/education_settings_label.json
  modified:
    - frappe-bench/apps/university_erp/university_erp/university_academics/tests/test_education_fork.py
    - frappe-bench/apps/university_erp/university_erp/university_academics/attendance.py (import rewrite)
    - frappe-bench/apps/university_erp/university_erp/university_academics/cbcs.py (import rewrite)
    - frappe-bench/apps/university_erp/university_erp/university_academics/timetable.py (import rewrite)
decisions:
  - "[03.3.1-02] fork_subset applied to 36 academics doctypes; 7 archive-flagged routed to _archived/ (quiz, question, quiz_question, quiz_activity, quiz_result, options, article)"
  - "[03.3.1-02] Education Settings doctype name kept (DB table stable); relabel achieved via Property Setter fixture (Frappe canonical pattern, mirrors ERPNext label overrides)"
  - "[03.3.1-02] Parallel execution: academics doctype/_archived/fixture artifacts landed via Plan 03 commit (03de577f) due to overlapping git operations; Plan 02 commit covers test scaffolding only"
metrics:
  duration: ~10min
  completed: 2026-05-14
  tasks_completed: 2
  files_created: 37
---

# Phase 03.3.1 Plan 02: Fork Academics Doctypes Summary

Wave 2 academics fork: 36 Education doctypes routed to `university_academics/` (29 active + 7 archived), all JSON `module` fields set to "University Academics", every `from education.` import rewritten to `from university_erp.<module>`, Education Settings relabel staged via Property Setter fixture, and the academics test scaffolding populated with the 29 kept doctype display names.

## What Was Built

### 1. Academics doctype fork (Task 1)
Invoked `fork_subset([... 36 academics doctypes ...])` via `university_erp.scripts.fork_education_module`. Result:
- **29 active doctypes** under `university_academics/doctype/`: academic_term, academic_year, assessment_criteria, assessment_criteria_group, assessment_group, assessment_plan, assessment_plan_criteria, assessment_result, assessment_result_detail, assessment_result_tool, course, course_activity, course_assessment_criteria, course_enrollment, course_schedule, course_scheduling_tool, course_topic, education_settings, grading_scale, grading_scale_interval, program, program_course, program_enrollment, program_enrollment_course, program_enrollment_fee, program_enrollment_tool, program_enrollment_tool_student, topic, topic_content
- **7 archived doctypes** under `university_academics/_archived/`: article, options, question, quiz, quiz_activity, quiz_question, quiz_result
- All JSON `module` fields verified as `"University Academics"`
- Zero `from education.` references remain anywhere under `university_academics/`
- Pre-existing module-level files (attendance.py, cbcs.py, timetable.py) had imports rewritten by `_walk_and_rewrite_module`

### 2. Education Settings relabel (Task 1, step 3)
Created `university_academics/fixtures/property_setter/education_settings_label.json` with a Property Setter record `Education Settings-main-label` setting `label = "Academics Settings"`. This applies on `bench migrate` (Plan 06) and keeps the doctype `name` (and `tabEducation Settings` table) unchanged per decision D-03.

### 3. Test scaffolding populated (Task 2)
Filled `DOCTYPES` list in `university_academics/tests/test_education_fork.py` with the 29 kept academics display names. Excluded archived ones (Quiz, Article, Options, Question, etc.). Test asserts each registered doctype has `module = "University Academics"` after `bench migrate` (Plan 06).

## Commits

| Task | Commit    | Description                                                                            |
| ---- | --------- | -------------------------------------------------------------------------------------- |
| 1    | 03de577f* | (academics doctype tree + fixture committed alongside Plan 03 due to parallel git contention) |
| 2    | 7270d369  | test(03.3.1-02): populate DOCTYPES for university_academics fork tests                 |

*Note: During parallel Wave 2 execution, three executor agents (02/03/04) raced for `.git/index.lock`. Plan 03's commit absorbed the academics doctype/_archived/fixtures artifacts because both agents had them staged at commit time. The file contents and verification all originate from Plan 02's `fork_subset` invocation; commit attribution is the only thing that drifted.

## Deviations from Plan

**[Parallel-execution] Commit attribution drift** — The plan expected one commit per task under the Plan 02 namespace. Due to parallel git index lock contention during Wave 2, Task 1's artifacts (academics doctype/, _archived/, fixtures/property_setter/, and module-level import rewrites) ended up included in commit `03de577f` (Plan 03's commit). The file content is correct; only the commit message namespace is misattributed. Task 2's test scaffolding landed cleanly under `7270d369` with the expected `test(03.3.1-02)` prefix.

## Acceptance Criteria — All Met

Task 1:
- [x] `grep -rn "from education\." university_erp/university_academics/ --include="*.py" | grep -v __pycache__` returns 0 hits
- [x] `university_academics/doctype/{course,program,assessment_result,academic_year,academic_term,education_settings}/` all exist
- [x] Property Setter fixture exists at `fixtures/property_setter/education_settings_label.json` with `value="Academics Settings"`, `doc_type="Education Settings"`, `property="label"`
- [x] JSON `module` field == `"University Academics"` for course, program, assessment_result, education_settings (verified)
- [x] Quiz suite (quiz, question, quiz_question, quiz_activity, quiz_result, options) all under `_archived/`
- [x] Article under `_archived/`

Task 2:
- [x] `test_education_fork.py` parses (ast.parse OK)
- [x] DOCTYPES contains 29 entries (>= 25 required)
- [x] "Course", "Program", "Assessment Result", "Education Settings" present
- [x] "Quiz", "Article" absent

## Hand-off to Wave 3 (Plan 05 + Plan 06)

- Plan 05 (override collapse) can now point `Course`, `Program`, `Assessment Result` overrides at `university_erp.university_academics.doctype.{course,program,assessment_result}` (MERGE override candidates flagged in DOCTYPE_MAPPING.md rows 9, 12, 35).
- Plan 06 will run `bench migrate` to register the 29 forked academics doctypes under module "University Academics" and apply the Property Setter to relabel Education Settings → Academics Settings in the UI.
- `verify_education_removable` (still 6 baseline violations from `overrides/*.py`) is unaffected by Plan 02 because academics fork only added imports of the form `from university_erp.*`.

## Self-Check: PASSED

Verified on disk:
- `university_academics/doctype/course/course.json` — FOUND, module="University Academics"
- `university_academics/doctype/program/program.json` — FOUND, module="University Academics"
- `university_academics/doctype/education_settings/education_settings.json` — FOUND, module="University Academics"
- `university_academics/_archived/quiz/quiz.json` — FOUND
- `university_academics/_archived/article/article.json` — FOUND
- `university_academics/fixtures/property_setter/education_settings_label.json` — FOUND, value=Academics Settings
- `university_academics/tests/test_education_fork.py` — FOUND, DOCTYPES has 29 entries
- Commit `7270d369` — FOUND in git log
- Commit `03de577f` (carrier for Plan 02 artifacts) — FOUND in git log
